{{> survey/survey-header}}

<main class="main survey-page">
    <!-- 흐린 배경 이미지 오버레이 -->
    <div class="background-overlay">
        <img src="/assets/images/room4.jpg" alt="배경이미지" class="main-large-img bg-fade-img" />
    </div>

    <!-- 상단 타이틀 -->
    <h1 class="page-title survey-title">방의 상세 특징을 선택해주세요</h1>
    
    <form class="survey-form">
        <!-- 슬라이더 (방 크기) -->
        <div class="slider-group">
            <label for="roomsize-slider" class="slider-label">방 크기</label>
            <div id="roomsize-slider"></div>
            <span id="roomsize-slider-values"></span>
        </div>

        <!-- 층수/옵션 -->
        <div class="survey-row room-option-row">
            <!-- 층수 선택 -->
            <div class="select-col">
                <label for="floor" class="select-label">층</label>
                <select id="floor" name="floor" class="main-select">
                    <option value="">전체</option>
                    <option value="1">1층</option>
                    <option value="2">2층 이상</option>
                    <option value="half">반지층</option>
                </select>
            </div>
            
            <!-- 옵션 토글 -->
            <div class="option-toggle-col">
                <label for="elevator" class="option-label">엘리베이터</label>
                <label class="switch">
                    <input type="checkbox" id="elevator" name="elevator">
                    <span class="switch-slider"></span>
                </label>
                <label for="parking" class="option-label">주차</label>
                <label class="switch">
                    <input type="checkbox" id="parking" name="parking">
                    <span class="switch-slider"></span>
                </label>
                <label for="option" class="option-label">옵션 여부</label>
                <label class="switch">
                    <input type="checkbox" id="option" name="option">
                    <span class="switch-slider"></span>
                </label>
            </div>
        </div>
    </form>

    <div class="survey-nav-cont">
        <button type="button" class="base-btn survey-nav-btn previous-btn" onclick="location.href='/survey/2'">&larr; Previous</button>
        <button type="button" class="base-btn survey-nav-btn next-btn" onclick="location.href='/survey/4'">Next &rarr;</button>
    </div>

    <!-- 좌측 하단 Home 버튼 -->
    <div class="side-nav">
        <button type="button" class="base-btn side-nav-btn" onclick="location.href='/'">Home</button>
    </div>
</main>

<script type="text/javascript" src="/js/survey/survey-roomdetail.js"></script>