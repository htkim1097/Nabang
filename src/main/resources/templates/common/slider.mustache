<div class="slider-group">
    <label for="{{id}}">{{label}}</label>
    <input type="range" id="{{id}}" name="{{name}}" min="{{min}}" max="{{max}}" value="{{value}}" step="{{step}}">
    <span class="slider-value" id="{{id}}-value">{{valueLabel}}</span>
</div>